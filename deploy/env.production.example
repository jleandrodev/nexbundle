# Copie para .env.production (na VPS) e preencha. NÃO versione o .env.production.
# Em produção o Shopify CLI NÃO injeta nada — você seta tudo aqui.

# --- Credenciais do app (Partners → app → API credentials / Client) ---
SHOPIFY_API_KEY=            # = client_id do app
SHOPIFY_API_SECRET=         # client secret

# --- URL pública do backend (seu domínio, com https) ---
# DEVE bater com application_url registrado no Dashboard e com app_proxy.url no toml.
SHOPIFY_APP_URL=https://app.seudominio.com

# --- Scopes (iguais ao shopify.app.toml) ---
SCOPES=read_products,write_discounts

# --- Banco: SQLite PERSISTENTE, fora do diretório de deploy (sobrevive a redeploy) ---
DATABASE_URL=file:/var/data/buy-together/prod.sqlite

# --- Porta local que o PM2/Remix escuta (o Nginx faz proxy pra ela) ---
PORT=3000
NODE_ENV=production

# --- App Review ---
# Lojas que recebem acesso liberado sem assinatura (o reviewer da Shopify pode
# testar numa loja que NÃO é development store e cairia no paywall).
# Lista separada por vírgula. ESVAZIE depois que o app for aprovado.
# Loja usada na review de 01/08/2026 ("Fabricator"):
REVIEW_SHOPS=ysghzc-g1.myshopify.com

# --- Suporte / chat ---
# Secret dedicado para assinar o cookie da equipe de atendimento (/support).
SUPPORT_SESSION_SECRET=troque-por-uma-string-aleatoria-longa
# Diretório PERSISTENTE dos anexos (fora do dir de deploy, mesmo volume do SQLite).
UPLOAD_DIR=/var/data/buy-together/uploads
